require 'spec_helper'

describe Musicassette do
  it 'has a version number' do
    expect(Musicassette::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
